@using Microsoft.AspNetCore.Identity
@using Petclinic.Entities

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    @* <li class="nav-item">
        <a id="manage" class="nav-link text-light" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
    </li> *@
    <li class="nav-item">
        <div class="dropdown">
            <a class="nav-link text-light" id="dropdownMenuButton" data-toggle="dropdown">
                <span style="margin-right:2px;">My Account</span>
                <img src="~/img/icons/down-arrow.svg" alt="arrow down" style="width:12px;margin-top:-3px;" />
            </a>
            <div class="dropdown-menu">
                    <a id="manage" class="d-block text-dark py-1 ml-3" asp-area="" asp-controller="/Owner" asp-action="Index" title="Manage">Owner Portal</a>
                    <a id="manage" class="d-block text-dark py-1 ml-3" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">User Settings</a>
            </div>
        </div>
     </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="button" onClick=showLogOutAlert() class="nav-link text-light">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link text-light" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-light" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}
</ul>
